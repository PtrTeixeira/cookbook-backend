server:
  type: default
  requestLog:
    appenders:
      - type: console
baseUrl: ${COOKBOOK_BASE_URL:-"http://localhost:8080"}
database:
  driverClass: org.h2.Driver
  user: sa
  password: sa
  url: jdbc:h2:./db
oauthConfig:
  clientId: ${COOKBOOK_CLIENT_ID}
