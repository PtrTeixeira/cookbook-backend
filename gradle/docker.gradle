apply plugin: 'com.bmuschko.docker-java-application'

docker {
  javaApplication {
    baseImage = 'openjdk:8u141-jre'
    maintainer = 'Peter Teixeira "PtrTeixeira@gmail.com"'
    ports = [8080, 8081]
    tag = "ptrteixeira/cookbook:$version"
  }
}

